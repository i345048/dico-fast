server:
  port: 8002
spring:
  application:
    name: api-gateway
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka
  instance:
    prefer-ip-address: true

management:
  security:
    enabled: false
#zuul:
#  routes:
#    ucenter:
#      path: /ucenter/**
#      stripPrefix: true
#      serviceId: ucenter
#    api-gateway:
#      path: /api-gateway/**
#      stripPrefix: true
#      serviceId: api-gateway
#  ignored-services: '*'

#饥饿加载
zuul:
  ribbon:
    eager-load:
      enabled: true

#feign:
#  hystrix:
#    enabled: true

#超时配置
#ribbon:
#  ReadTimeout:100
#  ConnectTimeout:500
#  MaxAutoRetries:1
#  MaxAutoRetriesNextServer:1


security:
  user:
    password: user  # 直接登录时的密码
  ignored: /
  sessions: never   # session策略
  oauth2:
    sso:
      loginPath: /login   # 登录路径
    client:
      clientId: ea470e327b1e50a690ae
      clientSecret: e249ec38aafc93f62d5b7f45b47cc9513665af8b
      accessTokenUri: https://github.com/login/oauth/access_token
      userAuthorizationUri: https://github.com/login/oauth/authorize
    resource:
      userInfoUri: https://api.github.com/user
      preferTokenInfo: false